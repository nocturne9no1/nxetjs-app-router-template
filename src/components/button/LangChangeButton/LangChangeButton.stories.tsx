import type { Meta, StoryObj } from '@storybook/react';
import { LangChangeButton } from './LangChangeButton';

const meta: Meta<typeof LangChangeButton> = {
	component: LangChangeButton,
	argTypes: {
		
}
	};

export default meta;
type Story = StoryObj<typeof LangChangeButton>;

export const Default: Story = {
	args: {
		
	}
};